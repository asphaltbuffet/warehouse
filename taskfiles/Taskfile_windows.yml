version: '3'

tasks:
  clean:
    desc: remove files created during build pipeline (windows)
    cmds:
    # this will return an error if the directory doesn't exist but we'll just ignore that
      - Powershell.exe Remove-Item -Path dist -Recurse
      - Powershell.exe Remove-Item -Path '.\COVERAGE.*'
    ignore_error: true
